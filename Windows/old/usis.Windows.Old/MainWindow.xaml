<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:usis.Windows"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="usis.Windows.MainWindow"
        Height="480" Width="640" Closed="Window_Closed">
    <DockPanel x:Name="DockPanel">
        <Menu x:Name="Menu" Height="{DynamicResource {x:Static SystemParameters.MenuBarHeightKey}}" DockPanel.Dock="Top">
            <MenuItem x:Name="MenuItemFile" Header="_File">
                <MenuItem Header="_Open..." Command="{Binding OpenDocumentCommand, ElementName=workspace, Mode=OneWay}"/>
                <MenuItem Header="_Close" Command="{Binding CloseDocumentCommand, ElementName=workspace, Mode=OneWay}"/>
                <Separator/>
                <MenuItem x:Name="MenuItemExit" Header="E_xit" Click="MenuItemExit_Click"/>
            </MenuItem>
        </Menu>
        <local:WorkspaceControl x:Name="workspace"/>
    </DockPanel>
</Window>
